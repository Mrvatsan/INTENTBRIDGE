Based on the current structured intent and conversation history, identify if there is any blocking ambiguity.
If yes, generate 1-3 clear, direct questions for the user to answer.
If no, return an empty list of questions and state that requirements are clear.

Structured Intent: {{structured_intent}}
History: {{history}}

Return JSON:
{
    "has_ambiguity": boolean,
    "questions": ["string"],
    "analysis": "string"
}
